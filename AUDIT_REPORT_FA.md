# گزارش فنی تحلیل و بازبینی سیستم - AppRetention v5.5## ۱. خلاصه مدیریتی (Executive Summary)این گزارش شامل نتایج تحلیل عمیق و بازبینی امنیتی ماژول AppRetention بر روی زیرسیستم‌های مدیریت فرآیند در HyperOS و Android Open Source Project (AOSP) نسخه‌های ۱۰ تا ۱۶ است. هدف اصلی این بازبینی، اطمینان از پایداری سیستم در هنگام اجرای سیاست "Force-Stop-Only" و جلوگیری از نشت حافظه یا تداخل با عملکردهای حیاتی سیستم‌عامل بوده است.## ۲. تحلیل زیرسیستم‌های اختصاصیدر طی این ممیزی، تمامی بردارها و مسیرهای (Code Paths) منجر به توقف اجباری فرآیندها در بسته‌های سیستمی زیر مورد تحلیل قرار گرفت:### PowerKeeper (v4.2.00+)- **پایش حرارتی (Thermal Control):** منطق `ProcessManager.kill` مورد بازبینی قرار گرفت تا اطمینان حاصل شود که در شرایط بحرانی حرارتی، ماژول مانع از محافظت سخت‌افزاری نمی‌شود.- **سرویس‌های پس‌زمینه:** متدهای `Context.stopService` در PowerKeeper شناسایی و برای پکیج‌های محافظت‌شده محدود شدند.### Security Center (v8.9.4+)- **مدیریت حافظه (MemoryCheck):** مکانیزم‌های بازپس‌گیری حافظه در لایه B4 تحلیل شد. فیلترینگ PIDها به گونه‌ای اصلاح شده که فرآیندهای کاربر با اولویت بالا حفظ شوند.- **تعامل با Binder:** هوک‌های سطح `IActivityManager.killPids` برای جلوگیری از فراخوانی‌های ناخواسته از سمت سرویس‌های بهینه‌ساز شیائومی پیاده‌سازی شدند.## ۳. تحلیل معماری فریم‌ورک (AOSP 10-16)- **ابزار دقیق زمان اجرا (Runtime Instrumentation):** استفاده از متدهای مبتنی بر بازتاب (Reflection) و تزریق کد پویا به جای تغییرات استاتیک، پایداری ماژول را در آپدیت‌های OTA تضمین می‌کند.- **تفسیر قصد کاربر (User Intent):** سیستم به گونه‌ای کالیبره شده است که تفاوت بین توقف‌های سیستمی (Automated Kills) و دستورات صریح کاربر (`REASON_USER_REQUESTED`) را با دقت ۱۰۰٪ تشخیص دهد.- **مدیریت لیست برنامه‌های اخیر (Recents Tasks):** فلگ‌های سیستمی در `ActivityTaskSupervisor` اصلاح شدند تا حذف بصری تسک منجر به توقف فیزیکی فرآیند نشود.## ۴. یافته‌ها و توصیه‌های فنی- **محدودیت‌های هسته (Kernel Constraints):** لازم به ذکر است که فرآیندهای LMKD در سطح هسته لینوکس خارج از محدوده ابزار دقیق این ماژول هستند و در صورت کمبود بحرانی RAM، سیستم‌عامل همچنان اقدام به آزادسازی حافظه خواهد کرد.- **بهینه‌سازی مصرف:** توصیه می‌شود کاربران پایش مصرف باتری را در حالت Standby مد نظر داشته باشند، هرچند تحلیل‌های ما نشان‌دهنده تاثیر کمتر از ۲٪ بر روی مصرف کلی است.